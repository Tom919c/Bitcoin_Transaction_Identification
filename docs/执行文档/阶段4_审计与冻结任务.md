请阅读当前项目全部文档和 Phase 4 工作汇报，尤其是：

1. PHASE4_WORK_REPORT.md
2. BTC_AML_FULL_DOCUMENTS_MERGED.md
3. experiments/route_validation/phase3_evidence_report.md
4. experiments/route_validation/next_route_recommendation.md
5. experiments/route_validation/final_route_after_phase4.md
6. experiments/ood_lite/ood_lite_gate_decision.md
7. experiments/paper_ready/paper_artifact_manifest.md
8. paper/tables/*.md
9. paper/draft/*.md
10. paper/figures/*.png

本轮任务是 Phase 4 Audit + Paper Route Freeze。

你必须注意：你是实验执行器，不是研究路线裁决者。不要继续扩展新模型，不要重写研究主线，不要直接生成最终论文。

当前总裁决如下：

1. 已成立、可以进入论文主贡献：

   * Sampling-aware 11-class Bitcoin entity benchmark
   * Label-preserving / class_balanced_khop / temporal_balanced 三套协议
   * Directed heterophily / unlabeled-neighbor dilution / hub exposure 机制分析
   * Direction-aware EGS 方法实证
   * Conservative sensitive ranking 应用贡献

2. 已发现但不能夸大：

   * Temporal OOD gap 仍未解决
   * Raw temporal edge features 不能作为方法贡献
   * Topology-aware reweighting Lite 未通过 OOD method gate
   * MIXER 暴露节点分类边界，但样本太少，不能立即做 subgraph GNN
   * Extended ranking 被 BET/GAMBLING 等容易类抬高，必须同时报告 conservative ranking

3. 最终路线暂定为：
   Benchmark + Mechanism + Direction-aware EGS + Conservative Ranking
   Temporal OOD 只写为 challenge / limitation / future work，不写为已解决方法贡献。

严禁事项：

* 不要实现新模型
* 不要继续调 topology-aware reweighting
* 不要实现 temporal edge filtering
* 不要实现 TGN
* 不要实现 causal GNN
* 不要实现 graph foundation model
* 不要实现 open-world / PU learning
* 不要实现 subgraph GNN
* 不要继续 focal loss gamma=2
* 不要直接生成最终投稿论文
* 不要生成最终 LaTeX 论文
* 不要修改标签语义
* 不要把 -1 当类别
* 不要把 INDIVIDUAL=0 当未标注
* 不要把 EXCHANGE 默认定义为非法或高风险

============================================================
Task 1: Phase 4 Completion Audit
================================

请检查并生成：

experiments/route_validation/phase4_completion_audit.md

该文件必须逐项核查以下文件是否存在、是否非空、内容是否合理：

Paper tables:

* paper/tables/table_dataset_protocols.md
* paper/tables/table_main_results.md
* paper/tables/table_ablation.md
* paper/tables/table_conservative_ranking.md
* paper/tables/table_temporal_feature_drift.md
* paper/tables/table_case_study_summary.md

Paper figures:

* paper/figures/fig_main_results_cbk.png
* paper/figures/fig_temporal_gap.png
* paper/figures/fig_ablation_direction_temporal.png
* paper/figures/fig_conservative_vs_extended_ranking.png
* paper/figures/fig_temporal_feature_drift.png
* paper/figures/fig_heterophily_unlabeled_ratio.png
* paper/figures/fig_case_study_confusion_counts.png

Paper drafts:

* paper/draft/problem_definition.md
* paper/draft/main_claims.md
* paper/draft/experiment_summary.md
* paper/draft/failure_analysis.md
* paper/draft/case_study_notes.md

OOD files:

* experiments/ood_lite/ood_lite_results.csv
* experiments/ood_lite/ood_lite_multiseed_summary.md
* experiments/ood_lite/ood_lite_gate_decision.md
* experiments/ood_lite/ood_lite_per_class_delta.csv

Route files:

* experiments/route_validation/final_route_after_phase4.md
* experiments/paper_ready/paper_artifact_manifest.md

如果 `ood_lite_per_class_delta.csv` 不存在，请补生成。
如果无法生成，请在 audit 中明确说明原因。

============================================================
Task 2: Run Tests
=================

请运行：

python -m pytest -q

并把结果写入：

experiments/route_validation/phase4_test_status.md

如果测试失败，不要继续生成论文正文。先报告失败原因。

============================================================
Task 3: Baseline Mismatch Investigation
=======================================

Phase 4 报告中出现 baseline mismatch：

之前 Phase 2 记录：

* EGS no-temporal-edge temporal seed42 Macro-F1 = 0.2675
* EGS no-temporal-edge CBK seed42 Macro-F1 = 0.6643

Phase 4 记录：

* temporal baseline no temporal seed42 Macro-F1 = 0.2312
* CBK baseline seed42 Macro-F1 = 0.6005

请调查这个差异，并生成：

experiments/route_validation/baseline_mismatch_investigation.md

必须检查：

1. 是否使用了同一数据文件
2. 是否使用了同一 model config
3. 是否使用了同一 training config
4. 是否使用了同一 random seed
5. 是否使用了同一 split
6. 是否使用了同一 evaluation script
7. 是否 checkpoint 选择不同
8. 是否 early stopping metric 不同
9. 是否 edge_attr / no-temporal-edge 设置不同
10. 是否使用了不同 hidden_dim / dropout / lr / weight_decay / epochs

必须给出结论：

A. Phase 4 baseline 与 Phase 2 baseline 是同一实验，可直接比较
B. Phase 4 baseline 与 Phase 2 baseline 不是同一实验，只能内部比较
C. 无法确认，需要保守处理

如果是 B 或 C，则 final route 中必须写明：
“Phase 4 OOD-lite 结果仅作为探索性负结果，不作为严格主实验结果。”

============================================================
Task 4: Correct OOD Gate Decision
=================================

请更新或生成：

experiments/ood_lite/ood_lite_gate_decision_corrected.md

必须同时按两个参照系判断：

Reference 1: Phase 4 internal baseline

* temporal baseline no temporal mean = 0.2433
* topo mean = 0.2483
* delta = +0.0051

Reference 2: Previous strongest known baseline

* temporal no-temporal-edge best = 0.2675
* topo mean = 0.2483
* delta = -0.0192

CBK reference:

* Phase 4 internal baseline = 0.6005
* topo = 0.6175
* internal delta = +0.0170
* previous strongest CBK no-temporal-edge = 0.6643
* topo vs strongest delta = -0.0468

最终结论必须写为：

Topology-aware reweighting Lite FAILS the method-contribution gate.
It does not reach +0.03 temporal Macro-F1 improvement.
Compared with the previous strongest known baseline, it may even underperform.
Therefore it must not be written as a method contribution.
Temporal OOD remains an open challenge / limitation.

============================================================
Task 5: Final Paper Route Freeze
================================

请更新：

experiments/route_validation/final_route_after_phase4.md

最终路线必须选择：

Benchmark + Mechanism + Direction-aware EGS + Conservative Ranking

不要选择：

Benchmark + Mechanism + Direction-aware EGS + Conservative Ranking + Lightweight OOD Method

文件必须包含以下部分：

## 1. Final Established Contributions

包括：

* Sampling-aware benchmark
* 11-class label-preserving protocol
* Directed heterophily / unlabeled-neighbor dilution
* Direction-aware EGS evidence
* Conservative sensitive ranking

## 2. Negative / Challenge Findings

包括：

* Temporal OOD unsolved
* Raw temporal edge features drift and hurt
* Topology-aware reweighting fails
* MIXER node-level boundary
* Extended ranking inflated by easy classes

## 3. What to Say in Paper

请明确写：

* 可以说什么
* 不能说什么

必须包含：

可以说：
“Direction-aware aggregation significantly improves Bitcoin entity classification under class-balanced protocols.”

不能说：
“Temporal OOD has been solved.”

可以说：
“Raw temporal edge features exhibit severe distribution drift and can hurt temporal generalization.”

不能说：
“Temporal features are an effective model contribution.”

可以说：
“Conservative sensitive ranking shows practical AML triage value.”

不能说：
“Extended ranking alone proves high-risk detection ability.”

## 4. Ready for Paper Draft?

请给出 YES / NO。

条件：

* 如果 pytest 通过
* paper artifacts 完整
* baseline mismatch 已解释
* OOD gate corrected
  则写 YES。

否则写 NO，并列出缺口。

============================================================
Task 6: Stop
============

完成以上任务后停止。

不要写最终论文正文。
不要扩展实验。
不要实现新模型。

最终回复必须包含：

1. pytest 结果
2. completion audit 路径
3. baseline mismatch investigation 结论
4. corrected OOD gate 结论
5. final route 文件路径
6. 是否建议进入论文正文生成
